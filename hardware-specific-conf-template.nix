{
  # Hostname
  #networking.hostName = "white";

  # Video Driver
  #services.xserver.videoDrivers = ["intel"];

  # CPU microcode
  #hardware.cpu.intel.updateMicrocode = true;

  # Bootloader device (if BIOS boot)
  #boot.loader.grub.device = '/dev/sda'
}
